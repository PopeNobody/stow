#!/bin/bash

ip-addr () 
{ 
    for i in "$@";
    do
        echo $i $(ip -br addr show "$i" | awk '{print $3}' | cut -d / -f 1);
    done
}
#exit if sourced, continuee if not.
return 0 2&>/dev/null
ip-addr "$@"
