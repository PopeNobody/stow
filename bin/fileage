#!/usr/bin/perl

use strict;
use warnings;
use Data::Dump;
use File::stat;
$\="\n";

my $t=time;
for(@ARGV)
{
  my $st=stat($_);
  if(!defined($st)){
    warn("stat:$_:$!");
    next;
  };
  printf "%d secs %d days: %s\n", ($t-$st->mtime), ($t-$st->mtime)/86400, $_;
};
